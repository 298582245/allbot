# 前端源码修改记录

本文件只记录 `web-ui/src` 下的打包前前端源码改动，不记录 `web/` 下构建产物。

## 2026-06-19

### `src/views/ScriptTasks.vue`

- 时间：14:07
- 修改范围：54-113、377-379、400-502
- 变更摘要：新增脚本任务手机端单列任务卡片表格，隐藏桌面大表并优化字段、操作按钮和分页横向滚动。
- 统计：新增 130 行，删除 2 行，净增 128 行。

### `src/views/ScriptEnvs.vue`

- 时间：14:20
- 修改范围：21-65、195-233
- 变更摘要：新增脚本环境变量手机端卡片列表；手机端隐藏桌面表格，搜索和按钮改为双列布局，弹窗表单改为纵向标签。
- 统计：该文件当前为新增源码文件，本次在既有页面基础上增加移动端结构和样式。

- 时间：14:27
- 修改范围：21-22、66-78、93、105-124、145-150、207-210、229-235
- 变更摘要：增加与脚本任务页一致的分页器；桌面表格和手机卡片均改用分页后的数据，搜索或切换每页条数时自动回到第一页。
- 统计：该文件当前为新增源码文件，本次改动在新增文件内追加分页逻辑和样式。

### `src/views/Dashboard.vue`

- 时间：14:20
- 修改范围：237-242、258-265、273、423-429
- 变更摘要：运行时间主值改为紧凑单位显示（Y/M/d/h/m/s），tooltip 保留中文完整时间，并限制主值单行省略。
- 统计：当前累计源码差异新增 73 行，删除 27 行，净增 46 行（包含本日该文件既有未提交改动）。

### `src/utils/request.js`

- 时间：14:20
- 修改范围：12-22、35-50
- 变更摘要：统一未授权处理；HTTP 401 或响应体为 `{ "error": "Unauthorized" }` 时自动退出登录并跳转登录页。
- 统计：新增 22 行，删除 12 行，净增 10 行。

### `src/views/Settings.vue`

- 时间：16:13
- 修改范围：27-32、177-178、270-271、378、382-394
- 变更摘要：Web UI 配置新增安全访问入口开关和访问码输入；加载设置映射后端访问码字段，保存前校验启用时访问码不能为空并 trim 后提交；修改密码后等待异步退出登录。
- 统计：新增 17 行，删除 1 行，净增 16 行。

### `src/stores/auth.js`

- 时间：16:13
- 修改范围：20-39
- 变更摘要：登出方法改为异步，支持控制是否调用登出接口和是否跳转；主动登出通过 fetch 调用 `/api/logout`，失败时仍清理本地登录状态。
- 统计：新增 12 行，删除 2 行，净增 10 行。

### `src/utils/request.js`

- 时间：16:13
- 修改范围：12-17
- 变更摘要：未授权处理改为不调用登出接口，避免 401 时递归触发 `/api/logout`。
- 统计：新增 1 行，删除 1 行，净增 0 行。

### `src/views/Layout.vue`

- 时间：16:13
- 修改范围：301-309
- 变更摘要：退出登录确认后等待异步登出流程完成。
- 统计：新增 1 行，删除 1 行，净增 0 行。

### `src/views/Adapters.vue`

- 时间：21:53
- 修改范围：97-108、194-195、291-302、607-625
- 变更摘要：在微信公众号适配器新增/编辑弹窗展示服务器配置回调地址，包含当前访问源、适配器 ID 和 callback_path，并提示明文模式与公网地址要求。
- 统计：新增 31 行，删除 0 行，净增 31 行。

## 2026-06-20

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：263-298、752-756、2196-2208
- 变更摘要：插件用户配置表单支持 `type: "divider"` 的 `user_config_schema` 字段，渲染为带标题和说明的分割线；普通配置项仍按原类型渲染。
- 统计：当前工作区该文件累计源码差异新增 187 行，删除 68 行，净增 119 行；本条仅记录分割线能力相关范围。

- 时间：当前会话
- 修改范围：266-269、2196-2208
- 变更摘要：分割线标题从左侧显示改为在配置输入区域居中显示，说明文字同步居中。
- 统计：当前工作区该文件包含此前累计改动；本条仅记录分割线标题居中样式调整。

## 2026-06-21

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：2196-2208
- 变更摘要：插件用户配置分割线改为整行铺满显示，标题仍居中，增强不同配置分组之间的区分度。
- 统计：当前工作区该文件包含此前累计改动；本条仅记录分割线整行显示样式调整。

### `src/views/PaymentOrders.vue`

- 时间：02:10
- 修改范围：195、202-203、405-417
- 变更摘要：支付订单详情的原始回调、事件消息和事件 payload 展示前自动解码 `\\uXXXX` Unicode 转义，并对可解析 JSON 进行格式化展示。
- 统计：新增 18 行，删除 3 行，净增 15 行。

### `src/views/ScriptTasks.vue`

- 时间：当前会话
- 修改范围：25-151、201-210、337-350、420-546
- 变更摘要：脚本任务列表、移动端卡片和日志弹窗新增运行总数、失败总数展示；电脑端将操作区拆为右侧独立固定栏并同步纵向滚动；手机端改为普通卡片列表，并将搜索区改为默认紧凑显示、高级筛选按需展开。
- 统计：当前工作区该文件本次改动包含统计字段展示、操作固定栏、移动端卡片修复和手机端搜索区压缩。

### `src/views/Statistics.vue`

- 时间：当前会话
- 修改范围：224-225、336-343
- 变更摘要：脚本运行统计卡片和成功率图表 tooltip 改为展示累计失败次数，配合后端累计运行口径。
- 统计：当前工作区该文件本次改动新增 2 行，删除 2 行，净增 0 行。

### `src/utils/request.js`

- 时间：当前会话
- 修改范围：1-23
- 变更摘要：401 未授权处理改为清理本地登录状态后显式 `router.replace('/login')`，避免间接登出跳转失效导致页面停留。
- 统计：新增 4 行，删除 2 行，净增 2 行。

## 2026-06-22

### `src/views/Dashboard.vue`

- 时间：01:25
- 修改范围：22-85、91-104、116-149、216-291、432-458、585-671、737-744
- 变更摘要：电脑端将消息分布图缩为左侧半宽卡片，右侧新增系统资源占用卡片；通过 CPU 和内存环形饼图展示实时占用百分比，并补充内存已用/总量展示和图表 resize/dispose 处理。
- 统计：新增 211 行，删除 7 行，净增 204 行。

## 2026-06-24

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：68-93、678、848-858、2111-2138
- 变更摘要：插件卡片指令展示限制为最多两行并通过悬浮提示查看完整内容；平台标签最多展示 3 个，多余平台聚合为 `+N` 并通过悬浮提示展示剩余平台名称，避免长指令和多平台撑高卡片。
- 统计：当前工作区该文件累计源码差异新增 102 行，删除 5 行，净增 97 行；本条记录插件卡片长内容压缩相关范围。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：349-356
- 变更摘要：新增 `setAdapterPinned(adapterId, pinned)` API 封装，通过适配器详情接口提交置顶和取消置顶动作。
- 统计：新增 8 行，删除 0 行，净增 8 行。

### `src/views/Adapters.vue`

- 时间：当前会话
- 修改范围：27-40、150、243-250、433-439、568-584
- 变更摘要：机器人卡片顶部新增独立一行置顶/取消置顶入口；列表在分页前按置顶状态排序；点击置顶后回到第一页并刷新列表，置顶卡片增加轻微边框提示。
- 统计：本次新增适配器置顶交互、排序和样式相关源码。

## 2026-06-23

### `src/api/index.js`

- 时间：当前会话
- 修改范围：135-138
- 变更摘要：新增 `setPluginPinned(pluginId, pinned)` API 封装，复用插件控制接口提交置顶和取消置顶动作。
- 统计：新增 5 行，删除 0 行，净增 5 行。

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：36-46、669、828-834、980-989、1037-1038、1152-1153、2048-2059、2376-2378
- 变更摘要：插件卡片顶部新增独立一行置顶/取消置顶入口，避免与插件名称重叠；列表在分页前按置顶状态排序；点击置顶后回到第一页并刷新列表，配置保存时保留 `pinned` 字段。
- 统计：当前工作区该文件累计源码差异新增 55 行，删除 2 行，净增 53 行；本条记录插件置顶交互相关范围。

### `src/views/Dashboard.vue`

- 时间：当前会话
- 修改范围：106-144、323-351、390-414
- 变更摘要：首页四个统计卡片改为大号展示当前/实时数据，小字展示总量/累计数据；运行时间、运行插件、在线机器人和今日消息同步调整 tooltip 口径。
- 统计：本次主要为展示口径调整，新增 `setPluginCountStat` 并替换原内联插件统计赋值。

- 时间：当前会话
- 修改范围：74-81、119-128、225-227、259-269、301-324
- 变更摘要：系统资源占用卡片主值和饼图改为 allBot CPU/内存口径，保留整机 CPU/内存为辅助说明，并新增紧凑字节占比格式。
- 统计：当前工作区该文件累计源码差异新增 278 行，删除 26 行，净增 252 行；本条仅记录 allBot 资源口径相关范围。

- 时间：当前会话
- 修改范围：225-258
- 变更摘要：资源环形图改为三段展示：allBot 占用、系统其他占用、未使用；CPU 和内存分别使用不同强调色，未使用保持灰白色。
- 统计：当前工作区该文件累计源码差异新增 282 行，删除 26 行，净增 256 行；本条仅记录三段资源环形图调整。

- 时间：当前会话
- 修改范围：237-266、281-286
- 变更摘要：资源环形图增加 allBot 视觉最小占比；allBot 真实占用在 0% 到 10% 之间时图形按 10% 展示，主数字和 tooltip 仍显示真实百分比。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录视觉最小占比调整。

- 时间：当前会话
- 修改范围：290-294
- 变更摘要：allBot 视觉最小占比规则覆盖 0% 场景，避免 CPU 真实占用显示为 0.00% 时环形图仍不可见。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录零占用视觉放大修正。

- 时间：当前会话
- 修改范围：237-266
- 变更摘要：修正资源环形图 data 误用真实 allBot 占比的问题，改为使用视觉放大后的 allBot 占比；系统其他占用也增加最小可见段，确保图上可见三种颜色。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录三段颜色可见性修正。

### `src/views/PluginEditor.vue`

- 时间：当前会话
- 修改范围：25-32、91-150、323-495
- 变更摘要：插件编辑页改为在页面主内容高度内布局，避免电脑端主容器超过屏幕；手机端插件目录增加收起/展开按钮，收起后隐藏目录树以释放代码编辑空间。
- 统计：新增 46 行，删除 5 行，净增 41 行。
