# 前端源码修改记录

本文件只记录 `web-ui/src` 下的打包前前端源码改动，不记录 `web/` 下构建产物。

## 2026-08-10
### `src/views/Plugins.vue`

- 时间：本次任务
- 修改范围：新建插件对话框、导入状态与文件选择处理、导入提交逻辑
- 变更摘要：保留原有模板创建流程，并在新增插件对话框增加本地插件导入模式；支持选择插件文件夹或 ZIP 压缩包，展示文件清单、大小、插件 ID 和校验诊断，提交后调用批量导入接口。
- 统计：本次改动新增 213 行、删除 4 行，净增 209 行；修改后共 2808 行。

### `src/api/index.js`

- 时间：本次任务
- 修改范围：178-186
- 变更摘要：新增本地插件文件夹或 ZIP 压缩包导入 API 封装，并配置较长上传超时。
- 统计：本次改动新增 11 行、删除 0 行，净增 11 行；修改后共 743 行。

### `src/utils/pluginImport.js`

- 时间：本次任务
- 修改范围：1-182
- 变更摘要：新增插件导入文件路径规范化、目录结构识别、插件清单与入口校验、ZIP 基础检查和诊断工具。
- 统计：新增文件，共新增 182 行；修改后共 182 行。

### `src/utils/pluginImport.test.js`

- 时间：本次任务
- 修改范围：1-99
- 变更摘要：新增插件导入纯函数测试，覆盖安全路径、包装目录、Node.js/Python 入口、清单错误、路径冲突和 ZIP 大小检查。
- 统计：新增文件，共新增 99 行；修改后共 99 行。

## 2026-08-02

### `src/views/WebChat.vue`

- 时间：01:20
- 修改范围：259、262、265、269、271、336、1246-1247
- 变更摘要：Web 聊天室的普通文本、富文本回退和按钮消息统一使用安全链接格式化，消息内全部符合规则的 HTTP/HTTPS 地址可点击打开；Markdown 文本同步支持裸链接，并扩展消息气泡链接样式。
- 统计：本次改动新增 8 行、删除 48 行，净减 40 行；修改后共 1304 行。

### `src/utils/webChatText.js`

- 时间：01:20
- 修改范围：1-152
- 变更摘要：新增 Web 聊天文本格式化工具，遍历全部 HTTP/HTTPS 链接，并结合常见域名后缀确定域名边界，避免链接吞入后续中文正文；支持识别无协议常见域名并默认以 HTTPS 打开，统一进行 HTML 转义、安全属性编码和 Markdown 显式链接处理。
- 统计：新增文件，共新增 152 行；修改后共 152 行。

### `src/utils/webChatText.test.js`

- 时间：01:20
- 修改范围：1-85
- 变更摘要：新增自动超链接单元测试，覆盖多链接完整匹配、中英文分隔符、协议链接后直接跟中文、无协议常见域名、邮箱排除、非链接 HTML 转义，以及 Markdown 链接、标记保护、平衡括号和链接文本转义。
- 统计：新增文件，共新增 85 行；修改后共 85 行。

### `package.json`

- 时间：01:20
- 修改范围：9
- 变更摘要：新增前端 `npm test` 脚本，将 Web 聊天文本格式化测试接入统一本地测试入口。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行；修改后共 30 行。

## 2026-07-23

### `src/stores/auth.js`

- 时间：23:50
- 修改范围：6-20、23-45、52-58
- 变更摘要：持久化管理员会话 CSRF Token，退出接口同时发送 Bearer 与 CSRF 请求头，并在本地注销时同步清理 CSRF Token。
- 统计：本次改动新增 14 行、删除 2 行，净增 12 行；修改后共 60 行。

### `src/utils/request.js`

- 时间：23:50
- 修改范围：34-36
- 变更摘要：为所有非 GET、HEAD、OPTIONS 的后台请求自动附加管理员 CSRF 请求头。
- 统计：本次改动新增 3 行、删除 0 行，净增 3 行；修改后共 75 行。

### `src/views/Login.vue`

- 时间：23:50
- 修改范围：102
- 变更摘要：登录成功后保存后端返回的管理员 CSRF Token。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行；修改后共 173 行。

### `src/views/PluginPanel.vue`

- 时间：16:11
- 修改范围：33-42
- 变更摘要：将 iframe 刷新地址改为带时间版本的虚拟目录，使入口页面中的相对 JS、CSS、图片和字体自动请求同一全新版本路径，插件无需修改资源引用即可绕过 iframe 子资源旧缓存。
- 统计：本次改动新增 5 行、删除 3 行，净增 2 行；修改后共 104 行。

- 时间：14:53
- 修改范围：13、27-39
- 变更摘要：为插件面板 iframe 增加响应式刷新标识和查询参数，初次加载及点击刷新时请求新的入口 URL，并保留 iframeKey 重建 iframe 上下文。
- 统计：本次改动新增 9 行、删除 1 行，净增 8 行；修改后共 102 行。

### `src/views/Plugins.vue`

- 时间：14:53
- 修改范围：261-279、1271-1278、1368-1381、2231-2260、2563-2572
- 变更摘要：在已有插件配置的 Web 聊天室区域增加快捷指令动态编辑、直接发送/填入输入框动作选择和移动端单列布局；加载与保存统一规范化快捷指令，兼容历史未知动作并保留 fill 文本尾随空格。
- 统计：本次改动新增 80 行、删除 2 行，净增 78 行；修改后共 2599 行。

### `src/views/WebChat.vue`

- 时间：12:56
- 修改范围：283、291、377-379、441、859-869
- 变更摘要：为 Web 聊天快捷指令增加可选的 `fill` 行为；标记为填入的快捷指令只写入并聚焦消息输入框，未标记的快捷指令保持原有直接发送行为；使用输入组件引用聚焦并区分动作类型。
- 统计：本次改动新增 16 行、删除 2 行，净增 14 行；修改后共 1347 行。

### `src/views/ScheduledTasks.vue`

- 时间：11:56
- 修改范围：215、281-311、344、353-357、485、487、603-634、637-673、887-901
- 变更摘要：将新增/编辑定时任务的用户 ID 改为按当前平台远程搜索的平台账号下拉，候选项展示用户 ID 与 UnionID，同时保留手动输入；增加请求序列、加载与空结果状态，避免平台切换或快速搜索时旧响应覆盖，并在弹窗关闭时清理候选状态。
- 统计：本次改动新增 124 行、删除 2 行，净增 122 行；修改后共 1054 行。

## 2026-07-21

### `src/views/PaymentConfig.vue`

- 时间：21:47
- 修改范围：31-34、260、262-268、412、470、482
- 变更摘要：在基础支付规则中新增按元展示的单笔支付上限，默认 9999.99 元，并在加载标准化和保存 payload 中精确换算为 `max_payment_amount_cents`；提取默认上限常量。
- 统计：本次改动新增 15 行、删除 0 行，净增 15 行；修改后共 593 行。

### `src/views/PaymentOrders.vue`

- 时间：21:47
- 修改范围：87-92、240-258、267-292、432-434、476-479、497
- 变更摘要：先加载订单列表再独立静默读取支付配置，配置失败时回退默认值且不阻断订单展示；RMB、积分列改为显式 slot 包裹并按管理员上限与当前页数据动态计算宽度，保留移动端卡片布局。
- 统计：本次改动新增 38 行、删除 7 行，净增 31 行；修改后共 545 行。

### `src/utils/request.js`

- 时间：21:47
- 修改范围：64
- 变更摘要：支持静默请求在网络错误时不弹出全局错误提示，供订单页非关键支付配置读取使用。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行；修改后共 72 行。

## 2026-07-20

### `src/api/index.js`

- 时间：19:06
- 修改范围：9-18
- 变更摘要：Web 聊天室请求兼容统一的 `code/msg/data` 响应，成功时透明返回业务数据，失败时优先展示 `msg` 并兼容旧错误字段。
- 统计：本次改动新增 6 行、删除 2 行，净增 4 行；修改后共 732 行。

### `src/utils/request.js`

- 时间：17:43
- 修改范围：39-63
- 变更摘要：识别并透明解包后台统一的 `code/msg/data` 响应，兼容统一错误消息和 401 登录过期处理，同时保留 Blob、ArrayBuffer 与附件下载响应的原始数据。
- 统计：本次改动新增 12 行、删除 5 行，净增 7 行；修改后共 72 行。

### `src/views/Dependencies.vue`

- 时间：17:43
- 修改范围：348
- 变更摘要：安装依赖失败时优先读取统一响应的 `msg`，并兼容原有 `error`、`message` 字段。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行；修改后共 661 行。

### `src/views/Layout.vue`

- 时间：17:43
- 修改范围：589、606、638、671
- 变更摘要：版本检查与升级状态错误展示兼容统一响应的 `msg`，并保留旧错误字段回退。
- 统计：本次改动新增 4 行、删除 4 行，净增 0 行；修改后共 1297 行。

### `src/views/Logs.vue`

- 时间：17:43
- 修改范围：258
- 变更摘要：日志下载的 Blob 错误解析兼容统一响应 `msg`，避免将 JSON 错误保存为下载文件。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行；修改后共 605 行。

### `src/views/OpenApiEditor.vue`

- 时间：17:43
- 修改范围：258
- 变更摘要：开放接口编辑器错误提示兼容后台统一响应 `msg`，同时保留旧协议字段回退。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行；修改后共 474 行。

### `src/views/PluginEditor.vue`

- 时间：17:43
- 修改范围：353、505、528、755-756、845、991、1040
- 变更摘要：插件文件与分类编辑的错误处理统一优先读取 `msg`，并复用错误解析函数兼容旧 `error`、`message` 和校验字段。
- 统计：本次改动新增 9 行、删除 9 行，净增 0 行；修改后共 1607 行。

### `src/views/Plugins.vue`

- 时间：17:43
- 修改范围：1124、1220
- 变更摘要：插件创建和备份删除失败提示兼容统一响应 `msg`，并保留原错误字段回退。
- 统计：本次改动新增 2 行、删除 2 行，净增 0 行；修改后共 2521 行。

### `src/views/Settings.vue`

- 时间：17:43
- 修改范围：300、326、357
- 变更摘要：系统更新检查和升级错误提示兼容统一响应 `msg`，并保留旧错误字段回退。
- 统计：本次改动新增 3 行、删除 3 行，净增 0 行；修改后共 683 行。

### `src/views/PluginEditor.vue`

- 时间：16:20
- 修改范围：1374-1424
- 变更摘要：将分类代码卡片改为填满分类内容区，代码编辑器随剩余高度弹性扩展并设置 420px 最小高度，使编辑框接近占满容器、尽量减少短代码滚动。
- 统计：相对仓库版本累计新增 1148 行、删除 84 行，净增 1064 行；修改后共 1607 行。

### `src/views/PluginEditor.vue`

- 时间：15:52
- 修改范围：235-261、513-543、682-710
- 变更摘要：彻底移除高级文件编辑中的 hybrid section 定位、变更映射和 `section_updates` 提交逻辑，所有文件统一只提交完整内容与打开时摘要，避免任何模板登记信息继续成为 plugin.json、脚本或未登记文件的在线编辑条件。
- 统计：相对仓库版本累计新增 1138 行、删除 84 行，净增 1054 行；修改后共 1597 行。

### `src/views/PluginEditor.vue`

- 时间：15:08
- 修改范围：7-83、147-179、192-321、364-635、680-843、949-1005、1070-1110、1139-1154、1181-1648
- 变更摘要：取消文件编辑页对文件类型、plugin.json、脚本文件和 hybrid ownership 的在线编辑限制，目录中的文件统一允许读取、保存、新建和删除；保存仍携带文件摘要用于并发提示，但不再要求 section updates；补齐卡片 body、editor body、tabs、pane、文件布局的 Flex 高度链和内部滚动，确保长表单、脚本和分类内容可以向下滚动。
- 统计：相对仓库版本累计变更以当前工作树为准，修改后共 1648 行。

### `src/views/PluginEditor.vue`

- 时间：14:49
- 修改范围：7-83、147-179、192-321、364-420、523-635、680-843、949-1005、1139-1154、1206-1505、1520-1648
- 变更摘要：完善分类编辑器的卡片内滚动和宽度约束，分类代码改为顶部标签条配合左右箭头逐类切换，切换时销毁并重建当前分类 CodeMirror；清理文件树 ownership、SHA 和基线展示，恢复 hybrid `patchable` 文件的在线完整编辑，通过 section 原文定位与变更映射提交完整文件和 section updates；保存后同步文件摘要、分类源和当前文件状态，继续保持 referenced、preserved、generated 只读以及分类编辑文件集合不可新建、删除。
- 统计：相对仓库版本新增 1189 行、删除 84 行，净增 1105 行；修改后共 1648 行。

### `src/views/PluginEditor.vue`

- 时间：01:37
- 修改范围：7-83、147-179、192-321、364-420、523-635、680-843、949-1005、1139-1154、1206-1337、1366-1505
- 变更摘要：完善 v2 hybrid 分类编辑：每个 section 使用按声明语言、文件后缀和插件运行时选择语言的独立 CodeMirror 6，并补齐只读、同步、切页销毁和卸载生命周期；基础字段与 section 统一参与变化检测、保存预览和白名单提交，无表单修改时不发送 PUT，409 仅提示重载且不覆盖重试；hybrid 已登记文件在高级页统一只读，patchable 文件仅能通过人工登记 section 修改，并禁止新建、删除，保留目录导出；普通文件保存携带打开时摘要以阻止覆盖外部变更，同时保持 v1 编辑、转换和覆盖流程不变。
- 统计：相对仓库版本累计新增 1042 行、删除 78 行，净增 964 行；修改后共 1507 行。

### `src/utils/accountQlTemplate.js`

- 时间：01:37
- 修改范围：170-195、208-250、253-309
- 变更摘要：补齐 parse、login、query、ck_check、auth、ql_registration、ql_env、schedules、routes、after_run 等 v2 section 分类中文；统一以 patchable 判定可编辑，说明性 `read_only_reason` 不再误锁 patchable；hybrid 模板源构建仅更新白名单 plugin 字段和原有 section content，并原样保留未知顶层字段、plugin 扩展字段、files 及 section 结构。
- 统计：相对上一条记录新增 141 行、删除 0 行，净增 141 行；修改后共 309 行。

## 2026-07-19

### `src/views/Layout.vue`

- 时间：15:35
- 修改范围：1178-1184
- 变更摘要：将电脑端侧栏版本弹窗中的当前版本号改为居中显示，并将字号由 26px 调整为 24px，保持其余视觉样式不变。
- 统计：本次改动新增 2 行、删除 1 行，净增 1 行；修改后共 1297 行。

### `src/api/index.js`

- 时间：21:28
- 修改范围：174-201
- 变更摘要：新增插件模板编辑模型探测、保存和旧账号模板显式转换 API 封装；旧模板转换固定提交 `convert_legacy: true` 与 `overwrite_generated_files: true`，并继续统一编码插件 ID、静默交由编辑页展示冲突与校验信息。
- 统计：截至本次修正，相对仓库版本累计新增 28 行、删除 0 行，净增 28 行；修改后共 728 行。

### `src/utils/accountQlTemplate.js`

- 时间：21:01
- 修改范围：1-168
- 变更摘要：抽取账号青龙模板判定、脚本运行时归一化、默认代码、默认表单、自定义指令、触发规则及账号配置归一化规则，供插件创建页和插件编辑页共同复用。
- 统计：新增文件，共新增 168 行、删除 0 行，净增 168 行；修改后共 168 行。

### `src/views/Plugins.vue`

- 时间：21:01
- 修改范围：749-761、883-890、1360-1380、1457-1482、1541-1543
- 变更摘要：将账号模板判断、默认账号表单、默认代码、运行时归一化和触发指令规则改为复用共享模块，保留创建页现有的响应式默认值与 CodeMirror 生命周期接口。
- 统计：本次改动新增 24 行、删除 107 行，净减 83 行；修改后共 2521 行。

### `src/views/PluginEditor.vue`

- 时间：21:28
- 修改范围：1-133、139-222、243-337、401-452、459-660、789-803、855-977
- 变更摘要：优先探测模板编辑模型，仅在文件编辑回退或进入文件/高级编辑页签时加载目录；可编辑账号模板提供基础、青龙、自定义代码、自定义指令及文件/高级编辑分类；旧账号模板返回 `requires_conversion` 时显示明确转换入口，使用 `conversion_source` 预填基础与青龙配置并清空四类代码和各路由代码，未手工补齐启用代码前阻止转换，取消转换仍保留文件编辑；普通保存改用 `overwrite_generated_files`，转换保存提交 `convert_legacy` 并确认覆盖生成文件；保存后同步生成文件状态并清理文件树缓存，补充入口文件和 `plugin.json` 由模板生成的提示，并根据后端错误数组、错误对象、文本或字段推断并切换校验页签。
- 统计：截至本次修正，相对仓库版本累计新增 636 行、删除 76 行，净增 560 行；修改后共 1103 行。

## 2026-07-18

### `src/api/index.js`

- 时间：20:32
- 修改范围：555-567
- 变更摘要：新增日志二进制下载 API，支持按日期或全部日志请求，保留 Blob 响应并使用静默错误处理和压缩下载长超时。
- 统计：本次改动新增 13 行、删除 0 行，净增 13 行；修改后共 700 行。

### `src/views/Logs.vue`

- 时间：20:32
- 修改范围：20-42、89、126-127、137、144、167-169、254-315、516-530
- 变更摘要：页头新增按当前日期或全部日志下载菜单，使用独立下载状态防止重复请求并禁用删除、清空和立即清理；通过 Blob 和临时下载链接保存原始日志或 ZIP，解析 JSON Blob 错误避免保存伪文件，并校验 ZIP 文件签名、延迟释放 Object URL，防止 HTML 等异常响应或下载读取竞态产生无效压缩包；移动端操作区调整为两列网格。
- 统计：本次改动新增 95 行、删除 8 行，净增 87 行；修改后共 605 行。

### `src/views/OpenApiEditor.vue`

- 时间：14:05
- 修改范围：301-313、447-451
- 变更摘要：参照插件编辑页将开放接口编辑页改为继承后台内容区的固定高度，补齐最小高度和溢出限制，并移除桌面端与移动端基于视口计算的高度，避免外层容器滚动与代码编辑器内部滚动叠加。
- 统计：本次改动新增 6 行、删除 2 行，净增 4 行；修改后共 474 行。

### `src/views/Layout.vue`

- 时间：20:00
- 修改范围：11-83、324、349-354、368-376、422-527、578-762、802-847、1123-1276
- 变更摘要：在桌面侧栏品牌区新增当前版本入口与紧凑更新卡片，版本标签去除 AllBot 前缀后横向显示在品牌文字右侧，并按已是最新或存在更新使用绿色、黄色背景区分；复用现有更新查询、升级状态和一键升级 API，支持并行初始化、刷新检查、升级确认、串行状态轮询、失败与不支持状态提示，并为安全打开更新日志的入口增加外链图标。
- 统计：本次改动新增 574 行、删除 5 行，净增 569 行；修改后共 1296 行。

## 2026-07-16

### `src/api/index.js`

- 时间：18:28
- 修改范围：309-333
- 变更摘要：新增开放接口全局 IP 设置读取与保存 API，以及按接口 ID 获取调用明细和服务端筛选分页的 API 封装，继续对路径参数使用 `encodeURIComponent`。
- 统计：本次改动新增 26 行、删除 0 行，净增 26 行；修改后共 687 行。

### `src/views/OpenApis.vue`

- 时间：18:28
- 修改范围：17、41-76、105-134、161-207、246-360、364-426、454-467、499-505、536-576、582-598、607-635、646-801、803-877、902-932、979-1431
- 变更摘要：新增全局 IPv4/IPv6/CIDR 白名单、可信代理和明细保留期设置；接口新增/编辑支持继承全局、允许全部和自定义三态策略，桌面表格与移动卡片展示实际 IP 策略和累计调用量；新增调用数据抽屉，提供永久累计摘要、结果/IP/状态码/时间筛选、服务端分页、本地化时间和移动端适配，并通过请求版本防止旧响应覆盖当前接口数据。
- 统计：本次改动新增 637 行、删除 27 行，净增 610 行；修改后共 1431 行。

### `src/views/Adapters.vue`

- 时间：00:49
- 修改范围：164-168
- 变更摘要：同步 QQ 第三方适配器 fallback schema，新增默认 NapCat 的框架下拉，明确 OneBot 11 正向通用 WebSocket 与可选 HTTP API 地址，并为两个通道提供独立访问令牌字段。
- 统计：本次改动新增 5 行、删除 2 行，净增 3 行。

### `src/views/WebChat.vue`

- 时间：15:24
- 修改范围：121、131、139-250、272-274、312、319-346、373-430、444-569、721-744、767-805、892-938、1140-1221
- 变更摘要：将手机端会话抽屉改为登录后先展示的独立会话列表与聊天双视图，绑定和退出收纳到列表菜单，聊天页支持返回且保持当前会话状态；统一初始化入口并增加会话消息、已读及退出竞态防护，按实际聊天可见性处理 SSE 未读和跨 820px 断点重载；快捷指令改为单行触摸横滑及溢出左右按钮，插件会话和切换卡片描述限制为两行。
- 统计：本次改动新增 282 行、删除 76 行，净增 206 行。

- 时间：15:49
- 修改范围：114-215、248-287、339-641、792-830、919-965、1192-1326
- 变更摘要：修复移动会话改造后的插件列表初始化与响应归一化，统计加载失败不再阻断插件和固定私聊；补全会话切换版本保护并避免跨断点重复加载；修复手机端聊天高度收缩链，确保消息区、输入框和发送按钮完整可见；插件会话及消息内插件卡片描述支持点击展开和收起，并清理失效展开状态。
- 统计：截至本次修复，`WebChat.vue` 相对当前仓库版本累计新增 392 行、删除 86 行，净增 306 行；修改后共 1329 行。

- 时间：16:44
- 修改范围：126-153、188-215、588-633、1205-1315
- 变更摘要：手机端点击会话后立即进入聊天并显示加载状态，请求失败时恢复原视图，避免网络等待和重复点击造成切换失效；将描述展开改为独立按钮，使描述正文点击仍可进入会话；聊天页面改为固定铺满视觉视口并压缩输入区高度、补充底部安全区，确保固定私聊的快捷指令、输入框和发送按钮完整可见。
- 统计：截至本次修复，`WebChat.vue` 相对当前仓库版本累计新增 394 行、删除 87 行，净增 307 行；修改后共 1330 行。

- 时间：16:52
- 修改范围：1243-1261、1296-1326
- 变更摘要：确认固定私聊与插件共用聊天主体后，将聊天主体从三行网格改为明确的纵向弹性高度链，消息区独占剩余空间，标题、快捷栏和输入区禁止收缩；手机端固定快捷栏及输入区高度，并让聊天主体直接固定到视口，避免固定私聊额外快捷指令把发送按钮挤出屏幕。
- 统计：截至本次修复，`WebChat.vue` 相对当前仓库版本累计新增 400 行、删除 92 行，净增 308 行；修改后共 1331 行。

## 2026-07-15

### `src/views/Dashboard.vue`

- 时间：23:02
- 修改范围：76-77、85-86、139-141、300-302、362-364
- 变更摘要：将原有资源峰值明确为“本次运行最高”，并新增从后端永久快照读取的 CPU 与内存“历史最高”；历史内存按持久化的字节数和百分比直接展示，避免跨机器或总内存变化后重新计算失真。
- 统计：本次改动新增 14 行、删除 2 行，净增 12 行。

## 2026-07-10

### `src/api/index.js`

- 时间：21:03
- 修改范围：416-467
- 变更摘要：新增用户聚合列表、用户详情、统一状态更新、后台积分调整、积分流水和平台账号列表 API 封装。
- 统计：本次改动新增 53 行、删除 0 行，净增 53 行。

### `src/router/index.js`

- 时间：21:03
- 修改范围：41
- 变更摘要：新增用户管理页面路由及页面标题。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行。

### `src/views/Layout.vue`

- 时间：21:03
- 修改范围：120-123、355
- 变更摘要：在桌面端平台配置菜单和移动端更多菜单中加入用户管理入口。
- 统计：本次改动新增 5 行、删除 0 行，净增 5 行。

### `src/views/Users.vue`

- 时间：21:03
- 修改范围：1-534
- 变更摘要：新增响应式用户管理页面，支持按 UnionID 聚合筛选、按平台浏览账号、异常绑定标记、用户详情与完整账号列表、积分流水、后台积分调整以及整个 UnionID 的封禁和解禁；列表和详情请求增加竞态防护。
- 统计：新增文件，共新增 534 行、删除 0 行，净增 534 行。

- 时间：21:52
- 修改范围：493-538
- 变更摘要：修复手机端固定高度和内部滚动导致卡片列表被压缩不可见的问题，改为随后台主内容自然滚动，并完善平台视图、卡片头部、详情抽屉和积分弹窗的移动端布局。
- 统计：本次追加新增 14 行、删除 9 行，净增 5 行；文件现共 539 行。

- 时间：21:56
- 修改范围：462-470、503-506
- 变更摘要：补齐桌面端标签页内容区的 Flex 高度和溢出链路，使 UnionID 表格占用剩余区域、分页器固定显示在表格下方；手机端继续保持自然滚动。
- 统计：本次追加新增 8 行、删除 6 行，净增 2 行；文件现共 541 行。

- 时间：当前会话
- 修改范围：465、505
- 变更摘要：修正对 Element Plus 活动标签面板类名的错误假设，直接让标签面板建立纵向 Flex 布局，确保 UnionID 表格不再把分页器挤出可视区域；手机端显式恢复块级自然布局。
- 统计：本次修改新增 1 行、删除 2 行，净减少 1 行；文件现共 540 行。

## 2026-07-07

### `src/views/Backups.vue`

- 时间：当前会话
- 修改范围：50-53、130-137、166-170、193-199、254-263、304-320、355-356
- 变更摘要：备份配置新增“运行环境与依赖”选择项，恢复弹窗新增对应恢复选项和自动下载说明；导入摘要可展示运行环境依赖内容，恢复完成弹窗会展示运行环境恢复告警。
- 统计：本次改动新增 13 行、删除 6 行，净增 7 行。

### `src/views/Dashboard.vue`

- 时间：当前会话
- 修改范围：75-82、129-132、281-284
- 变更摘要：系统资源占用卡片新增 AllBot CPU 与内存历史最高显示，便于观察运行期间峰值占用和排查异常状态。
- 统计：本次改动新增 8 行、删除 0 行，净增 8 行。

- 时间：当前会话
- 修改范围：75-84、132-140、284-292
- 变更摘要：系统资源占用卡片新增 AllBot CPU 与内存运行期平均占用显示，配合历史最高值观察高峰期和常态占用差异。
- 统计：本次改动新增 9 行、删除 0 行，净增 9 行。

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：207-259、822-826、1134-1141、1249-1252、1279-1283、1324-1345
- 变更摘要：在插件配置弹窗的“用户配置”和“插件配置”之间新增“Web配置”页签，支持维护插件 WebUI 面板显示、标题、入口、图标和排序；新增 Web 聊天室显示开关及入口标题、说明、输入提示、入口指令和搜索关键词配置，关闭后 Web 用户插件入口不再展示该插件。
- 统计：本次改动新增 87 行、删除 1 行，净增 86 行。

- 时间：当前会话
- 修改范围：162-166、388、2393-2420、2544-2554
- 变更摘要：插件配置弹窗增加专用样式类和主体滚动容器，限制弹窗在视口内显示；页签栏固定不滚动，仅当前页签内容滚动，保留底部保存/取消按钮常驻可见。
- 统计：本次追加新增 39 行、删除 1 行，净增 38 行。

### `src/views/PluginEditor.vue`

- 时间：当前会话
- 修改范围：33-44、102、110、123、230-256、442-446
- 变更摘要：插件目录标题栏在“新建”按钮左侧新增“导出”按钮，点击后携带后台 token 请求插件导出接口，按 `${pluginId}.zip` 以 blob 下载当前插件目录压缩包，并在导出期间显示 loading 状态。
- 统计：本次改动新增 49 行、删除 9 行，净增 40 行。

### `src/views/PaymentOrders.vue`

- 时间：当前会话
- 修改范围：26-30、49-53、92-96、130、176、431-439、462-463
- 变更摘要：订单筛选、列表卡片和详情中的订单状态改为中文展示；电脑端支付方式列加宽，避免 alipay_transfer 换行；状态标签取消最大宽度限制，避免 cancelled 状态后显示省略号。
- 统计：本次改动新增 16 行、删除 10 行，净增 6 行。

### `src/views/ScriptTasks.vue`

- 时间：当前会话
- 修改范围：22、329-371、427、433-435、447、502-512、544-560、739-747、863-871
- 变更摘要：页头改为仅展示“任务设置”入口，将日志保留天数、运行超时秒数和超时通知管理员开关移动到脚本任务设置弹窗；保存入口改为完整脚本任务设置保存，并在页面说明中补充超时从运行阶段开始计时、超时标记失败和管理员通知规则；自动刷新列表时不再覆盖已打开设置弹窗中的未保存输入。
- 统计：本次改动新增 87 行、删除 27 行，净增 60 行。

### `src/views/Logs.vue`

- 时间：当前会话
- 修改范围：404
- 变更摘要：为日志等级增加默认颜色兜底，避免异常等级或续行解析异常时文字颜色与深色背景过近。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：22-24
- 变更摘要：新增 WebChat 平台列表、平台验证码发送和平台验证码登录 API 封装。
- 统计：本次改动新增 3 行、删除 0 行，净增 3 行。

### `src/views/WebChat.vue`

- 时间：当前会话
- 修改范围：20-50、250-260、268-280、299、327、354-364、435-447、530-548、565、626-655
- 变更摘要：登录模式新增“平台验证码”，支持加载运行平台实例、选择平台用户 ID 或 UnionID、发送平台私聊验证码并使用验证码完成 WebChat 登录。
- 统计：本次改动新增 109 行、删除 3 行，净增 106 行。

- 时间：当前会话
- 修改范围：32-47、268、294、435-441、622-639
- 变更摘要：平台验证码登录二次调整为“Web 用户名 + 接收平台”，删除平台用户 ID / UnionID 输入，获取验证码提示改为不暴露账号绑定状态的泛化文案。
- 统计：本次改动新增 7 行、删除 12 行，净减 5 行。

- 时间：当前会话
- 修改范围：191-193、262-269、306-309
- 变更摘要：固定私聊会话新增 myid、积分充值、我的平台、绑定码、version 快捷指令按钮，进入固定私聊时可直接点击发送内置函数。
- 统计：本次改动新增 9 行、删除 3 行，净增 6 行。

## 2026-07-06

### `src/views/Layout.vue`

- 时间：当前会话
- 修改范围：17-23、247-280、366-378
- 变更摘要：侧栏菜单改为手动路由跳转，平台配置下的 Web 聊天入口点击后使用新浏览器标签页打开 `/chat`，其他后台菜单保持当前页内跳转。
- 统计：本次改动新增 9 行、删除 2 行，净增 7 行。

### `src/views/Logs.vue`

- 时间：当前会话
- 修改范围：286-290
- 变更摘要：日志页自动刷新定时器增加暂停滚动判断，开启“暂停滚动”后不再自动刷新日志列表，手动刷新仍可使用。
- 统计：本次改动新增 1 行、删除 1 行，净增 0 行。

### `src/views/PaymentConfig.vue`

- 时间：当前会话
- 修改范围：114-207、249-250、307-404、440、446、508
- 变更摘要：新增支付宝账单支付配置区块，支持网关、应用密钥、收款 UID、收款码地址、收银台公网地址、账单查询参数和金额唯一匹配模式配置；收款码地址支持上传图片在浏览器端解析二维码内容，并自动提取 `render.alipay.com` / `ds.alipay.com` 包装链接中的真实 `qr.alipay.com` 收款码；支付方式 provider 下拉和默认方式新增 `alipay_bill/alipay_transfer`；支付宝账单提示文案改为按入账金额匹配，同时间窗口重复金额自动加 0.01 元；支付方式归一化会自动补齐缺失的默认支付宝账单方式。
- 统计：本次改动新增约 180 行、删除 0 行，净增约 180 行；本次追加调整 约 15 行匹配模式、提示文案与默认方式补齐逻辑。

### `src/views/PaymentOrders.vue`

- 时间：当前会话
- 修改范围：240、331
- 变更摘要：订单管理说明改为可查询第三方待支付订单，并支持 `alipay_bill` pending 订单手动查询。
- 统计：本次改动新增 2 行、删除 2 行，净增 0 行。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：340-349
- 变更摘要：新增运行环境可下载候选版本 API 封装，使用静默 GET 请求透传 runtime、architecture、q、limit 参数。
- 统计：本次改动新增 10 行、删除 0 行，净增 10 行。

### `src/views/RuntimeProfiles.vue`

- 时间：当前会话
- 修改范围：157-180、247、267-271、334-344、376、394-395、406-408、425-476、692-695
- 变更摘要：自动下载模式目标版本改为可远程搜索、可手动创建的下拉选择；按运行时和架构加载候选版本，展示来源与资产名，下载设置变更后刷新候选，候选读取失败时保留手输保存能力。
- 统计：本次改动新增 102 行、删除 4 行，净增 98 行。

### `src/views/RuntimeProfiles.vue`

- 时间：当前会话
- 修改范围：160-168、252-271、290-304
- 变更摘要：运行环境架构选择新增 Linux x64/Linux ARM64，新增环境默认优先沿用后端已有默认架构；Python 自动下载允许 Linux 架构，并提示 Linux 下自动匹配下载 python-build-standalone 预编译包。
- 统计：本次改动新增 21 行、删除 4 行，净增 17 行；随后调整 Python managed 架构过滤和提示文案。

### `src/views/Settings.vue`

- 时间：当前会话
- 修改范围：31-33、581-583
- 变更摘要：在系统设置的访问码输入项下标注安全访问入口路径格式 `/login/访问码`，并提示开启后直接访问 `/login` 会返回 404。
- 统计：本次改动新增 3 行、删除 0 行，净增 3 行。

## 2026-07-05

### `src/api/index.js`

- 时间：当前会话
- 修改范围：17-21
- 变更摘要：新增 WebChat 邮箱验证码登录 API 封装，并沿用 `purpose` 参数发送登录验证码。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行。

- 时间：当前会话
- 修改范围：17-19
- 变更摘要：WebChat 邮箱验证码发送支持 `purpose` 参数，并新增找回密码重置接口封装。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行；另调整 1 行请求参数。

### `src/views/WebChat.vue`

- 时间：当前会话
- 修改范围：80-94、214-215、889-894
- 变更摘要：WebChat 手机端未登录认证底栏改为与后台手机底栏一致的图标+文字导航样式，去掉胶囊按钮观感。
- 统计：本次改动新增约 10 行、删除约 3 行，净增约 7 行。

- 时间：当前会话
- 修改范围：8-31、204-219、241-246、341-363、420-440、834-841
- 变更摘要：登录页新增“密码登录 / 邮箱验证码”切换，邮箱验证码登录可发送 `login` 用途验证码并完成免密码登录；手机端隐藏顶部认证页签，改用底部登录/注册/找回密码切换栏。
- 统计：本次改动新增约 64 行、删除约 4 行，净增约 60 行。

- 时间：当前会话
- 修改范围：824-827
- 变更摘要：手机端未登录认证页改用固定动态视口高度和页面内部滚动，绕过全局 `html/body` 禁止滚动限制，避免注册/找回密码表单内容过高时无法继续下滑。
- 统计：本次改动调整 2 行移动端样式。

- 时间：当前会话
- 修改范围：46-65、197-209、214-241、326-395
- 变更摘要：认证区新增“找回密码”页签，独立维护重置密码表单；支持发送 `reset_password` 验证码、校验新密码一致性、调用重置密码接口并成功后切回登录。
- 统计：本次改动新增约 67 行、删除 0 行，净增约 67 行。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：24
- 变更摘要：新增 WebChat 消息数统计接口封装，用于按会话恢复固定私聊和插件会话消息总数。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行。

- 时间：当前会话
- 修改范围：25
- 变更摘要：新增 WebChat 标记已读接口封装，用于把当前会话已读水位写入后端。
- 统计：本次改动新增 1 行、删除 0 行，净增 1 行。

### `src/views/WebChat.vue`

- 时间：18:22
- 修改范围：88、164-170、211-224、416-461、575-577、732-737、768-769
- 变更摘要：为 Markdown 围栏代码块新增右上角“展开”按钮，通过消息列表事件委托读取代码文本并用弹窗展示完整代码；弹窗底部新增“复制全部”，复制优先使用 Clipboard API 并保留 textarea 兜底，同时补充代码块按钮和弹窗代码区样式。
- 统计：本次改动后文件 771 行，相对上次记录净增约 57 行；当前文件为未跟踪源码文件，精确累计差异以工作区文件为准。

- 时间：当前会话
- 修改范围：6、50-68、86-124、138-154、172-208、234-254、268-270、320-326、348-379、397-402、467-469
- 变更摘要：新增固定私聊置顶会话，默认进入固定私聊；按空 `plugin_id` 加载和发送普通私聊消息，插件会话继续隔离；SSE 按会话分流并维护当前浏览器未读数和可刷新恢复的消息总数。
- 统计：本次改动后文件 516 行，相对修改前 463 行净增 53 行；当前文件为未跟踪源码文件，精确累计差异以工作区文件为准。

- 时间：当前会话
- 修改范围：130-132、346-372、513-514、528
- 变更摘要：发送消息时先在当前会话本地插入用户指令，后端返回后再替换为持久化消息，避免机器人回复先于用户指令显示；输入区改为输入框和发送按钮同一行，发送按钮固定在右侧。
- 统计：本次改动后文件 531 行，相对上次记录净增约 15 行。

- 时间：当前会话
- 修改范围：94、99、458-469
- 变更摘要：图片消息和富文本图片加载完成后再次滚动到底部，并在滚动函数中增加下一帧和短延迟滚动兜底，避免异步内容撑高后只滚动一小段。
- 统计：本次改动后文件 539 行，相对上次记录净增约 8 行。

- 时间：当前会话
- 修改范围：93、98、449-503、563-570
- 变更摘要：WebChat Markdown 消息从原样代码块显示改为渲染标题、列表、加粗、斜体和行内代码，富文本内 Markdown 片段同样按 Markdown 渲染，并对 HTML 做转义后再生成安全内容。
- 统计：本次改动后文件 601 行，相对上次记录净增约 62 行。

- 时间：当前会话
- 修改范围：449-529、539-551、617-620
- 变更摘要：补全 WebChat Markdown 的 fenced code block 和链接语法渲染；链接仅允许 http、https 和站内开放图片路径，并为外链添加 noopener noreferrer。
- 统计：本次改动后文件 652 行，相对上次记录净增约 51 行。

- 时间：当前会话
- 修改范围：210-218、422-424
- 变更摘要：插件会话列表按未读数优先排序，有未读消息的插件自动靠前；无未读时按会话标题中文排序，固定私聊继续保持置顶。
- 统计：本次改动后文件 660 行，相对上次记录净增约 8 行。

- 时间：当前会话
- 修改范围：177、197、247-262、332-336、375-378、402-410、432-450
- 变更摘要：WebChat 未读状态改为以后端已读水位为准；加载消息统计时恢复服务端未读数，进入当前会话和当前会话收到新消息后调用标记已读接口，刷新页面不会清空未进入会话的未读小红点。
- 统计：本次改动后文件 695 行，相对上次记录净增约 35 行。

- 时间：当前会话
- 修改范围：198、213-224、247、258-263、337、419-421、469-478
- 变更摘要：插件会话排序改为显式缓存刷新；点击未读会话后当前会话标记已读但位置保持不动，等下一次切换会话或非当前会话收到新未读时再刷新排序，避免选中项立即下沉。
- 统计：本次改动后文件 714 行，相对上次记录净增约 19 行。

### `src/views/WebChat.vue`

- 时间：当前会话
- 修改范围：79-125、419-425
- 变更摘要：将插件快捷指令从消息区顶部移动到输入框上方，并删除“用户消息仅支持文本；机器人回复支持 Markdown、图片、按钮和富文本。”提示文案。
- 统计：本次改动约新增 3 行、删除 4 行，净减 1 行；另调整 2 行相关布局样式。

## 2026-07-04

### `src/views/Dependencies.vue`

- 时间：当前会话
- 修改范围：143-145、314-322
- 变更摘要：Python 依赖安装包名校验允许 extras 语法（如 `qrcode[pil]`），并在输入占位和错误提示中说明示例。
- 统计：本次改动新增约 7 行，删除约 3 行。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：1-37
- 变更摘要：新增 Web 聊天室公开 API 封装，支持邮箱验证码、注册登录、绑定码、插件指令、消息、图片上传和退出登录。
- 统计：本次改动新增约 36 行，删除 0 行。

### `src/router/index.js`

- 时间：当前会话
- 修改范围：4-10
- 变更摘要：新增无需后台管理员登录的 `/chat` Web 聊天室路由。
- 统计：本次改动新增 6 行，删除 0 行。

### `src/views/Layout.vue`

- 时间：当前会话
- 修改范围：109-119
- 变更摘要：平台配置菜单新增 Web 聊天入口，便于管理员从后台打开聊天室。
- 统计：本次改动新增 4 行，删除 0 行。

### `src/views/WebChat.vue`

- 时间：当前会话
- 修改范围：1-459
- 变更摘要：新增 Web 聊天室页面，支持邮箱验证码注册、账号/邮箱登录、可选绑定码、插件指令列表、文本/Markdown/图片/富消息发送、SSE 实时接收和移动端布局。
- 统计：新增 459 行，删除 0 行，净增 459 行。

- 时间：当前会话
- 修改范围：72-107、126-326、328-372
- 变更摘要：用户发送端改为仅支持文本；机器人回复端保留 Markdown、图片、按钮和富文本渲染，并兼容后端富文本 `{ parts: [...] }` 数据结构，修复网页只显示时间不显示富文本内容的问题。
- 统计：本次改动删除用户侧 Markdown/图片/富消息编辑入口和上传逻辑，新增富文本对象结构兼容渲染；当前文件累计差异以工作区为准。

- 时间：当前会话
- 修改范围：49-131、142-371、374-425
- 变更摘要：Web 聊天室从全局指令列表改为插件独立会话入口，新增插件搜索、选中插件后按 `plugin_id` 加载独立消息、插件快捷指令、SSE 按插件分流和未读提示。
- 统计：本次改动重构 `/chat` 页面结构和状态管理，当前文件从 374 行增加到 427 行，净增约 53 行。

- 时间：当前会话
- 修改范围：89-105、225-242、430-438
- 变更摘要：新增跨插件命中建议卡片，点击后仅切换目标插件会话；普通图片和富文本图片改用 `el-image`，支持点击放大预览。
- 统计：本次改动新增约 31 行，删除约 4 行；当前文件为未跟踪源码文件，精确累计差异以工作区文件为准。

- 时间：当前会话
- 修改范围：443-445
- 变更摘要：调整按钮消息样式，使按钮宽度撑满消息框内容区域，文字居中并加大字号。
- 统计：本次改动新增 2 行，删除 1 行，净增 1 行。

### `src/views/ScriptTasks.vue`

- 时间：当前会话
- 修改范围：164-256、392-399、564-566、725-736
- 变更摘要：桌面端脚本任务操作按钮从独立悬浮操作面板改为表格内固定右侧操作列，移除滚动同步和固定行高对齐逻辑，避免按钮视觉位置与实际任务行错位导致暂停到其他任务。
- 统计：本次改动删除独立操作面板、滚动同步引用和相关样式，新增表格固定操作列及按钮间距样式；当前工作区该文件累计差异新增约 45 行，删除约 105 行。

## 2026-07-03

### `src/router/index.js`

- 时间：当前会话
- 修改范围：39-40
- 变更摘要：新增插件面板列表页 `/plugin-panels` 路由，保留单插件 iframe 面板路由。
- 统计：本次改动新增 1 行，删除 0 行，净增 1 行；当前工作区该文件累计差异新增 2 行，删除 0 行。

### `src/views/Layout.vue`

- 时间：当前会话
- 修改范围：55-63、307-318、335-354、450-468
- 变更摘要：将侧栏插件 WebUI 从逐个插件入口调整为固定“插件面板”入口，数量徽标紧跟“插件面板”文字显示并保持红色强调；手机端更多菜单也只保留聚合入口，避免插件多时侧栏过长。
- 统计：本次改动基于既有布局改造，当前工作区该文件累计差异新增约 200 行，删除 43 行。

### `src/views/PluginPanels.vue`

- 时间：当前会话
- 修改范围：14-24、34-48、64-75
- 变更摘要：插件面板聚合页新增按插件标题和插件 ID 搜索，空结果时展示提示，便于插件 WebUI 较多时快速定位。
- 统计：本次改动新增搜索相关约 18 行；当前文件为新增页面。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：89-96
- 变更摘要：新增插件 Web 面板列表 API 封装，用于动态发现已启用插件面板。
- 统计：当前工作区该文件累计差异新增 96 行，删除 4 行，净增 92 行；本条记录插件 Web 面板 API 相关范围。

### `src/router/index.js`

- 时间：当前会话
- 修改范围：39
- 变更摘要：新增 `/plugin-panels/:pluginId` 路由，加载通用插件面板 iframe 页面。
- 统计：当前工作区该文件累计差异新增 1 行，删除 0 行，净增 1 行。

### `src/stores/pluginWebPanels.js`

- 时间：当前会话
- 修改范围：1-30
- 变更摘要：新增插件 Web 面板 Pinia store，负责加载、缓存和按插件 ID 查找面板配置。
- 统计：新增 30 行，删除 0 行，净增 30 行。

### `src/views/Layout.vue`

- 时间：当前会话
- 修改范围：55-63、263-284、312-324、350-352
- 变更摘要：侧边栏和手机端更多菜单动态追加插件 Web 面板入口，并在插件面板路由中显示插件标题。
- 统计：当前工作区该文件累计差异新增 185 行，删除 43 行，净增 142 行；本条记录插件 Web 面板入口相关范围。

### `src/views/PluginPanel.vue`

- 时间：当前会话
- 修改范围：1-94
- 变更摘要：新增通用插件面板页面，通过 iframe 加载插件提供的静态 WebUI，并提供面板标题、插件 ID 和刷新按钮。
- 统计：新增 94 行，删除 0 行，净增 94 行。

## 2026-07-02

### `src/api/index.js`

- 时间：当前会话
- 修改范围：295-311
- 变更摘要：新增运行环境下载设置 GET/PUT API 封装，用于读取和保存代理、Node.js 镜像、Python 包镜像与 NuGet 元数据地址。
- 统计：本次改动新增 17 行，删除 0 行，净增 17 行。

### `src/views/RuntimeProfiles.vue`

- 时间：当前会话
- 修改范围：15-17、156-158、195-217、230、243-247、259-274、319-338
- 变更摘要：运行环境页面新增“下载设置”入口和弹窗，支持加载、恢复默认与保存下载代理/镜像配置；同步调整自动下载说明文案。
- 统计：本次改动新增 62 行，删除 3 行，净增 59 行。

- 时间：当前会话
- 修改范围：584-586
- 变更摘要：手机端页面头部按钮区改为两列网格，让“下载设置”和“新增环境”在同一行显示。
- 统计：本次改动新增 1 行，删除 0 行，净增 1 行。

## 2026-07-01

### `src/views/ScriptTasks.vue`

- 时间：当前会话
- 修改范围：78-89、132-152、231-245、302-315、574-598、619-625
- 变更摘要：脚本任务页的状态筛选、标签展示、暂停按钮和日志自动刷新逻辑新增 `queued` 排队中状态支持；排队任务在列表与详情中按活跃任务显示结束时间占位，并允许从任务页直接发起暂停/删除操作。
- 统计：当前工作区该文件本次改动新增排队状态支持与按钮可用性调整。

### `src/views/Settings.vue`

- 时间：当前会话
- 修改范围：52-60、173-184、262-277、383-396
- 变更摘要：系统设置页插件配置区新增脚本并发上限输入项，加载和保存 `script_task_concurrent_limit`，保存前确保最小值为 1；保存系统基础设置时只提交本页负责字段，避免覆盖权限页维护的平台管理员和访问控制白名单。
- 统计：当前工作区该文件本次改动新增脚本任务并发上限配置入口，并修复设置页保存误覆盖权限配置的问题。

## 2026-06-30

### `src/api/index.js`

- 时间：当前会话
- 修改范围：462-484
- 变更摘要：新增备份包导入和备份恢复 API 封装，分别设置 10 分钟与 15 分钟请求超时。
- 统计：当前工作区该文件累计差异新增 70 行，删除 4 行，净增 66 行；本条记录备份导入与恢复 API 相关范围。

### `src/views/Backups.vue`

- 时间：当前会话
- 修改范围：15-19、91-135、141-162、215-260、292-299、337-342、390-400
- 变更摘要：备份中心新增导入备份按钮、上传 zip 校验、导入摘要弹窗、备份恢复操作、恢复内容选择与覆盖确认，并在恢复成功后提示重启 AllBot。
- 统计：当前工作区该文件累计差异新增 149 行，删除 12 行，净增 137 行；本条记录备份导入与恢复交互相关范围。

## 2026-06-19

### `src/views/ScriptTasks.vue`

- 时间：14:07
- 修改范围：54-113、377-379、400-502
- 变更摘要：新增脚本任务手机端单列任务卡片表格，隐藏桌面大表并优化字段、操作按钮和分页横向滚动。
- 统计：新增 130 行，删除 2 行，净增 128 行。

### `src/views/ScriptEnvs.vue`

- 时间：14:20
- 修改范围：21-65、195-233
- 变更摘要：新增脚本环境变量手机端卡片列表；手机端隐藏桌面表格，搜索和按钮改为双列布局，弹窗表单改为纵向标签。
- 统计：该文件当前为新增源码文件，本次在既有页面基础上增加移动端结构和样式。

- 时间：14:27
- 修改范围：21-22、66-78、93、105-124、145-150、207-210、229-235
- 变更摘要：增加与脚本任务页一致的分页器；桌面表格和手机卡片均改用分页后的数据，搜索或切换每页条数时自动回到第一页。
- 统计：该文件当前为新增源码文件，本次改动在新增文件内追加分页逻辑和样式。

### `src/views/Dashboard.vue`

- 时间：14:20
- 修改范围：237-242、258-265、273、423-429
- 变更摘要：运行时间主值改为紧凑单位显示（Y/M/d/h/m/s），tooltip 保留中文完整时间，并限制主值单行省略。
- 统计：当前累计源码差异新增 73 行，删除 27 行，净增 46 行（包含本日该文件既有未提交改动）。

### `src/utils/request.js`

- 时间：14:20
- 修改范围：12-22、35-50
- 变更摘要：统一未授权处理；HTTP 401 或响应体为 `{ "error": "Unauthorized" }` 时自动退出登录并跳转登录页。
- 统计：新增 22 行，删除 12 行，净增 10 行。

### `src/views/Settings.vue`

- 时间：16:13
- 修改范围：27-32、177-178、270-271、378、382-394
- 变更摘要：Web UI 配置新增安全访问入口开关和访问码输入；加载设置映射后端访问码字段，保存前校验启用时访问码不能为空并 trim 后提交；修改密码后等待异步退出登录。
- 统计：新增 17 行，删除 1 行，净增 16 行。

### `src/stores/auth.js`

- 时间：16:13
- 修改范围：20-39
- 变更摘要：登出方法改为异步，支持控制是否调用登出接口和是否跳转；主动登出通过 fetch 调用 `/api/logout`，失败时仍清理本地登录状态。
- 统计：新增 12 行，删除 2 行，净增 10 行。

### `src/utils/request.js`

- 时间：16:13
- 修改范围：12-17
- 变更摘要：未授权处理改为不调用登出接口，避免 401 时递归触发 `/api/logout`。
- 统计：新增 1 行，删除 1 行，净增 0 行。

### `src/views/Layout.vue`

- 时间：16:13
- 修改范围：301-309
- 变更摘要：退出登录确认后等待异步登出流程完成。
- 统计：新增 1 行，删除 1 行，净增 0 行。

### `src/views/Adapters.vue`

- 时间：21:53
- 修改范围：97-108、194-195、291-302、607-625
- 变更摘要：在微信公众号适配器新增/编辑弹窗展示服务器配置回调地址，包含当前访问源、适配器 ID 和 callback_path，并提示明文模式与公网地址要求。
- 统计：新增 31 行，删除 0 行，净增 31 行。

## 2026-06-29

### `src/views/ScriptEnvs.vue`

- 时间：13:40
- 修改范围：16、28-31、57-59、92-97、118、135-138、216-237、263-266、283-285
- 变更摘要：脚本环境变量页新增置顶开关、桌面端和移动端图钉展示；保存 payload 显式提交 pinned，搜索支持匹配置顶/未置顶状态，并补充置顶说明。
- 统计：新增 53 行，删除 26 行，净增 27 行。

- 时间：当前会话
- 修改范围：18-45、84-89、119、144-147、155-170、199-268、303-352、378-408、431、454-459、464-465
- 变更摘要：脚本环境变量页新增导入变量、桌面端序号与多选、移动端复选框与序号、选中批量删除/启停/置顶/取消置顶/导出，并补充同名变量读取提示。
- 统计：当前工作区该文件累计差异新增 252 行，删除 35 行，净增 217 行；本条记录批量操作、导入导出和同名提示相关范围。

- 时间：当前会话
- 修改范围：45、84、88
- 变更摘要：脚本环境变量列表序号改为直接显示数据库 ID，配合后端按 ID 倒序展示。
- 统计：当前工作区该文件包含本日累计改动；本条仅记录序号展示调整。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：200-216
- 变更摘要：新增脚本环境变量批量操作与导入 API 封装。
- 统计：当前工作区该文件累计差异新增 50 行，删除 4 行，净增 46 行；本条记录脚本环境变量 API 相关范围。

## 2026-06-25

### `src/views/Statistics.vue`

- 时间：18:40
- 修改范围：197、384-414、474-485
- 变更摘要：消息总量趋势支持私聊和群聊双序列堆叠柱状图；兼容旧后端 totals 字段，并在 tooltip 中展示合计、私聊和群聊数量。
- 统计：新增 36 行，删除 10 行，净增 26 行。

## 2026-06-20

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：263-298、752-756、2196-2208
- 变更摘要：插件用户配置表单支持 `type: "divider"` 的 `user_config_schema` 字段，渲染为带标题和说明的分割线；普通配置项仍按原类型渲染。
- 统计：当前工作区该文件累计源码差异新增 187 行，删除 68 行，净增 119 行；本条仅记录分割线能力相关范围。

- 时间：当前会话
- 修改范围：266-269、2196-2208
- 变更摘要：分割线标题从左侧显示改为在配置输入区域居中显示，说明文字同步居中。
- 统计：当前工作区该文件包含此前累计改动；本条仅记录分割线标题居中样式调整。

## 2026-06-21

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：2196-2208
- 变更摘要：插件用户配置分割线改为整行铺满显示，标题仍居中，增强不同配置分组之间的区分度。
- 统计：当前工作区该文件包含此前累计改动；本条仅记录分割线整行显示样式调整。

### `src/views/PaymentOrders.vue`

- 时间：02:10
- 修改范围：195、202-203、405-417
- 变更摘要：支付订单详情的原始回调、事件消息和事件 payload 展示前自动解码 `\\uXXXX` Unicode 转义，并对可解析 JSON 进行格式化展示。
- 统计：新增 18 行，删除 3 行，净增 15 行。

### `src/views/ScriptTasks.vue`

- 时间：当前会话
- 修改范围：25-151、201-210、337-350、420-546
- 变更摘要：脚本任务列表、移动端卡片和日志弹窗新增运行总数、失败总数展示；电脑端将操作区拆为右侧独立固定栏并同步纵向滚动；手机端改为普通卡片列表，并将搜索区改为默认紧凑显示、高级筛选按需展开。
- 统计：当前工作区该文件本次改动包含统计字段展示、操作固定栏、移动端卡片修复和手机端搜索区压缩。

### `src/views/Statistics.vue`

- 时间：当前会话
- 修改范围：224-225、336-343
- 变更摘要：脚本运行统计卡片和成功率图表 tooltip 改为展示累计失败次数，配合后端累计运行口径。
- 统计：当前工作区该文件本次改动新增 2 行，删除 2 行，净增 0 行。

### `src/utils/request.js`

- 时间：当前会话
- 修改范围：1-23
- 变更摘要：401 未授权处理改为清理本地登录状态后显式 `router.replace('/login')`，避免间接登出跳转失效导致页面停留。
- 统计：新增 4 行，删除 2 行，净增 2 行。

## 2026-06-22

### `src/views/Dashboard.vue`

- 时间：01:25
- 修改范围：22-85、91-104、116-149、216-291、432-458、585-671、737-744
- 变更摘要：电脑端将消息分布图缩为左侧半宽卡片，右侧新增系统资源占用卡片；通过 CPU 和内存环形饼图展示实时占用百分比，并补充内存已用/总量展示和图表 resize/dispose 处理。
- 统计：新增 211 行，删除 7 行，净增 204 行。

## 2026-06-24

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：68-93、678、848-858、2111-2138
- 变更摘要：插件卡片指令展示限制为最多两行并通过悬浮提示查看完整内容；平台标签最多展示 3 个，多余平台聚合为 `+N` 并通过悬浮提示展示剩余平台名称，避免长指令和多平台撑高卡片。
- 统计：当前工作区该文件累计源码差异新增 102 行，删除 5 行，净增 97 行；本条记录插件卡片长内容压缩相关范围。

### `src/api/index.js`

- 时间：当前会话
- 修改范围：349-356
- 变更摘要：新增 `setAdapterPinned(adapterId, pinned)` API 封装，通过适配器详情接口提交置顶和取消置顶动作。
- 统计：新增 8 行，删除 0 行，净增 8 行。

### `src/views/Adapters.vue`

- 时间：当前会话
- 修改范围：27-40、150、243-250、433-439、568-584
- 变更摘要：机器人卡片顶部新增独立一行置顶/取消置顶入口；列表在分页前按置顶状态排序；点击置顶后回到第一页并刷新列表，置顶卡片增加轻微边框提示。
- 统计：本次新增适配器置顶交互、排序和样式相关源码。

## 2026-06-23

### `src/api/index.js`

- 时间：当前会话
- 修改范围：135-138
- 变更摘要：新增 `setPluginPinned(pluginId, pinned)` API 封装，复用插件控制接口提交置顶和取消置顶动作。
- 统计：新增 5 行，删除 0 行，净增 5 行。

### `src/views/Plugins.vue`

- 时间：当前会话
- 修改范围：36-46、669、828-834、980-989、1037-1038、1152-1153、2048-2059、2376-2378
- 变更摘要：插件卡片顶部新增独立一行置顶/取消置顶入口，避免与插件名称重叠；列表在分页前按置顶状态排序；点击置顶后回到第一页并刷新列表，配置保存时保留 `pinned` 字段。
- 统计：当前工作区该文件累计源码差异新增 55 行，删除 2 行，净增 53 行；本条记录插件置顶交互相关范围。

### `src/views/Dashboard.vue`

- 时间：当前会话
- 修改范围：106-144、323-351、390-414
- 变更摘要：首页四个统计卡片改为大号展示当前/实时数据，小字展示总量/累计数据；运行时间、运行插件、在线机器人和今日消息同步调整 tooltip 口径。
- 统计：本次主要为展示口径调整，新增 `setPluginCountStat` 并替换原内联插件统计赋值。

- 时间：当前会话
- 修改范围：74-81、119-128、225-227、259-269、301-324
- 变更摘要：系统资源占用卡片主值和饼图改为 allBot CPU/内存口径，保留整机 CPU/内存为辅助说明，并新增紧凑字节占比格式。
- 统计：当前工作区该文件累计源码差异新增 278 行，删除 26 行，净增 252 行；本条仅记录 allBot 资源口径相关范围。

- 时间：当前会话
- 修改范围：225-258
- 变更摘要：资源环形图改为三段展示：allBot 占用、系统其他占用、未使用；CPU 和内存分别使用不同强调色，未使用保持灰白色。
- 统计：当前工作区该文件累计源码差异新增 282 行，删除 26 行，净增 256 行；本条仅记录三段资源环形图调整。

- 时间：当前会话
- 修改范围：237-266、281-286
- 变更摘要：资源环形图增加 allBot 视觉最小占比；allBot 真实占用在 0% 到 10% 之间时图形按 10% 展示，主数字和 tooltip 仍显示真实百分比。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录视觉最小占比调整。

- 时间：当前会话
- 修改范围：290-294
- 变更摘要：allBot 视觉最小占比规则覆盖 0% 场景，避免 CPU 真实占用显示为 0.00% 时环形图仍不可见。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录零占用视觉放大修正。

- 时间：当前会话
- 修改范围：237-266
- 变更摘要：修正资源环形图 data 误用真实 allBot 占比的问题，改为使用视觉放大后的 allBot 占比；系统其他占用也增加最小可见段，确保图上可见三种颜色。
- 统计：当前工作区该文件累计源码差异新增 295 行，删除 26 行，净增 269 行；本条仅记录三段颜色可见性修正。

### `src/views/PluginEditor.vue`

- 时间：当前会话
- 修改范围：25-32、91-150、323-495
- 变更摘要：插件编辑页改为在页面主内容高度内布局，避免电脑端主容器超过屏幕；手机端插件目录增加收起/展开按钮，收起后隐藏目录树以释放代码编辑空间。
- 统计：新增 46 行，删除 5 行，净增 41 行。

## 2026-06-26

### `src/views/PluginEditor.vue`

- 时间：20:25
- 修改范围：43-50、119-120、181-184
- 变更摘要：插件编辑页首次加载入口文件时，同步设置目录树当前节点，确保直接进入 `/plugins/:id/edit` 时 `main.js` 或 `main.py` 正常高亮。
- 统计：新增 3 行，删除 0 行，净增 3 行。

## 2026-06-27

### `src/api/index.js`

- 时间：当前会话
- 修改范围：374-415
- 变更摘要：日志 API 封装支持查询参数和按日期删除，新增日志保留天数设置读取/保存与立即清理接口。
- 统计：新增日志历史管理相关 API 方法，替换原无参数日志读取和清空封装。

### `src/views/Statistics.vue`

- 时间：21:39
- 修改范围：184、240、254、493-559、584
- 变更摘要：存储占用对比新增日志文件占用项；概览接口返回的 logs 数据参与空状态判断和 treemap 渲染，三类存储项保留真实大小展示并增加最小可见占比。
- 统计：当前工作区该文件累计源码差异新增 217 行，删除 63 行，净增 154 行；本条仅记录存储对比加入日志文件相关范围。

### `src/views/Logs.vue`

- 时间：当前会话
- 修改范围：1-473
- 变更摘要：系统日志页改为磁盘历史日志查看流程，新增日期选择、等级筛选、服务端关键词搜索、分页、当前日期删除、全部清空、保留天数设置和立即清理；保留黑底日志展示与重复日志合并开关。
- 统计：重构原日志页模板、脚本与样式以接入历史日志和清理能力。

### `src/views/ScheduledTasks.vue`

- 时间：当前会话
- 修改范围：108-135、198-219
- 变更摘要：定时任务列表将状态字段移动到来源之后、表达式之前；移动端任务信息卡同步调整字段顺序。
- 统计：仅调整现有字段展示顺序，无新增业务逻辑。

- 时间：当前会话
- 修改范围：679-687
- 变更摘要：精简定时任务伪造身份展示，去掉过长的用户 ID，仅保留平台和机器人实例信息。
- 统计：删除用户 ID 拼接展示，净减少 3 行。

### `src/views/KeywordReplies.vue`

- 时间：当前会话
- 修改范围：29-33、120-135、226-235、271-279
- 变更摘要：关键字回复列表中的备注改为单行截断展示并支持悬浮查看全文；编辑弹窗里的备注输入改为多行文本域，与回复内容保持一致。
- 统计：新增少量展示辅助逻辑与样式，未改变数据结构。
